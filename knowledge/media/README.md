# Media & assets

Visual artifacts applications and events keep asking for. Subfolders:

- `headshots/` — professional photos (note date + resolution; conferences
  ask for these constantly). TODO(jossuema): add a current headshot.
- `talks/` — slides, poster PDFs, event photos, named by talk slug to
  match [`../talks/`](../talks/) (e.g. `cvpr-2024-latinx-poster.pdf`).
  TODO(jossuema): archive CVPR 2024 poster/abstract and FLISOL 2026
  slides.
- `projects/` — screenshots and demo stills, reusable by
  [`../../website/`](../../website/) and referenced from
  [`../../data/projects.yaml`](../../data/projects.yaml).

Naming: `<slug>-<what>.<ext>`, dates in filenames where relevant.
